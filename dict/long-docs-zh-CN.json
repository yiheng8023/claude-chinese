{
  "When off, the SSH option is hidden": "关闭时，SSH 选项将被隐藏，且任何连接尝试都将被拒绝。\n\n条目为精确主机名（`build01.corp.example.com`）或 `*.` 通配符（`*.corp.example.com` 可匹配任意深度的根域名及子域名）；匹配不区分大小写并忽略 `user@` 前缀。用户输入的主机与其 `~/.ssh/config` 解析出的 HostName 必须同时匹配，因此别名无法访问名单之外的主机。当解析的主机匹配时允许使用 ProxyCommand（此键用于控制应用提供的主机，而非网络出口）；ProxyJump 将被拒绝并提示使用 ProxyCommand。\n\n此功能为主动开启机制，因为远程会话会在 SSH 主机上运行 Claude Code，且应用会将该会话的推理凭据转发给它，并在设置了 otlpEndpoint 时转发 OTLP 收集器端点和认证标头。请仅列出您信任这些凭据的主机。基于 Token 的凭据会被转发；基于文件的类型（Bedrock IAM 身份中心登录或 AWS 配置文件、Vertex Google 登录或凭据文件）将在会话启动时被拒绝。\n\n若未设置此键，设备上 Claude Code 自身托管设置文件中的 sshHostAllowlist 仍然适用；当两者同时设置时，若应用的配置为管理员托管（MDM、管理控制台或设备托管的引导 URL），则此键优先，否则仅在该文件未设置时适用。allowedWorkspaceFolders 在远程主机上仍然生效。",
  "When enabled, Cowork, Code, and Chat sessions": "启用时，Cowork、Code 和 Chat 会话预先仅将工具名称放入上下文中，Claude 在首次需要使用某工具时才会获取其完整结构定义（Schema）。当配置了大量 MCP 工具且它们内联的结构定义占满了上下文窗口时（导致会话每隔一两轮就需要压缩上下文），请使用此功能。仅当您的端点能够转发并接受该请求结构时才启用它；若无法接受，请求将因 HTTP 400 失败。保持未设置以保留保守默认值。\n\n• **网关提供商，绑定 Claude Code 2.1.247 或更高版本的应用**：请求仅添加 tool-search 结构（tool-search-tool-2025-10-19 anthropic beta 值、延迟工具加载、tool_reference 内容块）；所有其他实验性 Claude Code Beta 均保持静默。保持该静默或关闭 ENABLE_TOOL_SEARCH 的操作系统级 Claude Code 托管设置仍然有效；可在其中设置 ENABLE_TOOL_SEARCH 以强制开启（配合 parentSettingsBehavior: \"merge\"）。使 Claude Code 处于自身网关模式（CLAUDE_CODE_USE_GATEWAY）的环境无论如何都会获得具有 Claude Code 网关安全结构的工具搜索。\n\n• **其他提供商及早期应用版本**：为该会话解除实验性 Beta 静默，因此请求会携带工具搜索结构以及 Claude Code 针对该提供商的其他实验性 Beta（例如上下文管理字段）。在使用绑定早于 2.1.221 的 Claude Code 版本的 Vertex 上，使用早于 Claude 4.5 的任何模型时请保持此项未设置；这些引擎无论模型为何都会发送该标头，而 Vertex 4.5 之前的技术栈会拒绝该标头。",
  "When the app launches under this organization": "当应用在此组织下启动时，通过“设置 → 导入”所用的同一导入程序，在后台将此计算机早期的第三方会话（在早期配置下或由早期内部版本写入）复制到其会话存储中（新会话 ID，但之前从 Claude.ai 导入的对话除外，后者保留其原始 ID；原会话保留在原位）。在设备分发配置（设备管理、配置文件或引导 URL）下每台设备最多运行一次，并在设备加入组织托管配置后再次运行一次。在组织托管配置下，它会从任何早期设备配置（包括通过控制台登录创建的配置）获取会话，但绝不会获取存储在另一个组织托管配置下的会话；在设备分发配置下，它仅获取在未配置组织 ID 时存储的会话，且仅在配置了组织 ID 后运行一次。同时开启“已启用”，以便用户可以手动导入遗漏的任何内容。需要 Cowork 在宿主机上运行（而非在虚拟机沙箱中）。",
  "Allows users to write this computer's chats": "允许用户将此计算机的对话、Cowork 任务和 Code 会话导出为便携式 ZIP 文件，以便其他安装可通过“设置 > 导入”进行导入。转录稿包含对话内容（包括工具输出）；仅会话记录元数据会投影到固定的字段允许名单中。请将导出的 ZIP 视为敏感数据对待。",
  "Only needed when the IdP completes sign-in": "仅当 IdP 从授权 URL 以外的主机完成登录时才需要。当回调被 CORS 策略阻止时，请设置包含授权服务器主机的 URL。任何带有该标头的来源都将被接受。",
  "- **`browser`** (default)": "- **`browser`**（默认）— 打开系统浏览器，在回环重定向端口上进行授权码（PKCE）登录。\n- **`device-code`** — 显示可在手机或另一台计算机上输入的代码（无需回环重定向）。\n- **`integrated`** — 在应用内显示 IdP 的登录表单（隐藏导航栏）。仅在配置了外部 IdP 时受支持。",
  "Absolute path to an executable that prints a single JSON object of environment variables": "可执行文件的绝对路径，该程序向标准输出（stdout）打印单个包含环境变量的 JSON 对象，例如 `{\"API_KEY\": \"...\"}`。Claude 会将这些变量注入到推理请求环境中。退出代码必须为 `0`。",
  "Sent on every inference and model-discovery request": "在每次推理和模型发现请求时发送（合并到 CLI 的 `ANTHROPIC_CUSTOM_HEADERS` 中）。\n\n可用于路由、租户区分或与上游代理的身份验证。",
  "Claude runs the executable with no arguments": "Claude 无参运行该可执行文件并读取其**标准输出**（stdout，经两端去空）。退出代码必须为 `0`；**标准错误**（stderr）上的任何输出都将导致请求中止。",
  "**External IdP mode.**": "**外部 IdP 模式。** 应用会发现 `<issuer>/.well-known/openid-configuration`，运行 OIDC 带有 PKCE 的授权码登录以获取 ID 令牌，然后将该断言交换为 AWS 凭据。",
  "Raises how long Cowork, Chat and Code sessions wait": "延长 Cowork、Chat 和 Code 会话在打开的流式响应中等待下一个模型事件的时间（Claude Code 内部称为流式空闲超时）。当上游代理或模型提供商对响应进行缓冲时，请使用此设置。",
  "Tier availability varies by model and region": "套餐可用性因模型和区域而异。预留容量使用预配吞吐量 ARN 作为模型 ID，而非默认的按需模型 ID。预配吞吐量在应用外部进行管理。",
  "`arn:aws:iam::<account>:role/<name>`": "`arn:aws:iam::<account>:role/<name>`。该角色的信任策略必须将插件的 Entra 应用程序作为 Web 身份予以接受。",
  "- **`device-code`** (default)": "- **`device-code`**（默认）— 显示在 microsoft.com/devicelogin 输入的代码。应用注册必须启用了公共客户端流。\n- **`browser`** — 在回环重定向端口上打开系统浏览器。使用此项可在组织提供的浏览器配置文件中单点登录。",
  "`inference-credential` adds `Authorization: Bearer <token>`": "`inference-credential` 会使用应用当前为所选推理提供商持有的令牌，向每次导出添加 `Authorization: Bearer <token>`。仅当提供商的令牌已是有效的 Bearer 令牌时才适用。",
  "Absolute path to an executable that prints a single JSON object of HTTP headers": "可执行文件的绝对路径，该程序向标准输出（stdout）打印包含 HTTP 标头的单个 JSON 对象，例如 `{\"Authorization\": \"Bearer ...\"}`。退出代码必须为 `0`；标准错误（stderr）上的任何输出都将导致请求中止。",
  "Extra resource attributes to attach to every span": "附加到发送至收集器的每个 span、指标和日志的额外资源属性。当启用终端用户属性归属时，`enduser.id` 会根据登录身份自动发出。适用于 Cowork 任务和 Code 会话。",
  "Each category enables a class of raw content": "每个类别可在发送至收集器的 OpenTelemetry 事件中启用一类原始内容（此数据绝不会发送给 Anthropic）。原始内容可能包含敏感数据。",
  "Enables Claude Code's session tracing": "在派生的 Code 会话中启用 Claude Code 的会话链路追踪（`CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1` + `OTEL_TRACES_EXPORTER=otlp`）。仅在启用 Code 模式时生效。",
  "Requires `inferenceTokenWindowHours` to also be set": "需要同时设置 `inferenceTokenWindowHours` — 没有窗口长度时上限处于非活动状态，不会执行任何限制。",
  "Required when `inferenceMaxTokensPerWindow` is set": "当设置了 `inferenceMaxTokensPerWindow` 时必填 — 只有两者均配置后上限才会生效。",
  "Also enables inline data analysis": "同时启用内联数据分析。沙箱只能读取附加到对话中的文件，无法访问任何网络。",
  "1P builds default to enabled at runtime": "第一方版本在运行时默认启用，除非显式设置。在第三方模式下，启用此项允许加载扩展；未设置或为 false 将禁用它们。",
  "Has no effect when `disableAutoUpdates` is in place": "启动时若启用了 `disableAutoUpdates` 则此项无效：更新程序根本不会启动，因此不会下载任何内容，也不会显示任何通知。",
  "By default the app asks `api.anthropic.com` which version": "默认情况下，应用会向 `api.anthropic.com` 询问要安装哪个版本。该主机也提供模型 API 服务，因此阻止非模型端点的组织可以将更新程序元数据重定向到此处。",
  "Covers the version-shipped announcement UI": "覆盖每个版本中内置的发版公告界面：更新后自动显示的**“新功能”**按钮，以及它打开的发布亮点面板。",
  "Set this to use a separate identity provider": "设置此项可使用单独的身份提供商（Microsoft Entra ID、Okta、Ping 或任何合规的 OIDC 提供商）进行引导登录步骤。",
  "Auto-populate the model picker from the provider's model-list": "启动时从提供商的模型列表端点自动填充模型选择器。对于网关和 Anthropic 提供商，缺失此键时默认启用。",
  "When set, each fetch or clone is rejected": "设置后，若插件市场的 `manifest.name` 不符，则每次获取或克隆都会被拒绝，从而防止上游重命名无感知地重定向现有市场注册。",
  "Locks per-tool permissions on MCP servers": "锁定通过 org-plugins 目录引入的 MCP 服务器的逐工具权限 — 每个服务器名称一个条目。若不存在，则由用户逐工具进行选择。",
  "Optional detail shown after the deployment display name": "显示在账户菜单标头中部署显示名称后面的可选详细信息（例如，“Claude 专享版”）。",
  "When the organization's configuration uses a field that is deprecated": "当组织的配置使用了已弃用的字段（重命名的键、旧值或旧条目格式）时，应用会在桌面 UI 中弹出横幅，以帮助管理员在支持终止前及时察觉。",
  "Each statement names an `effect`": "每个语句都指定一个 `effect`（`allow` 或 `deny`）、其覆盖的 `action` 功能标识符（`disabledFeatures` 词汇表）以及可选条件。",
  "The app activates 3P mode only when this is set": "应用仅在此项已设置且所选提供商所需的凭据密钥存在且非空时，才会激活第三方模式。",
  "Static headers sent on every request to the bootstrap config URL": "发送到引导配置 URL 的每个请求中的静态标头 — 用于服务账号凭据或租户令牌。",
  "Served by your configuration endpoint (remote bootstrap configuration)": "由您的配置端点提供服务（远程引导配置）；不从 MDM 读取。用于动态拉取最新的企业配置策略。",
  "When a user has no saved selection, start the picker on the 1M-context": "当用户没有已保存的选择时，如果默认模型提供 1M 上下文变体，则选择器默认启动在 1M 上下文变体上。",
  "Use the **provider's exact model ID**": "使用**提供商的精确模型 ID**：Vertex 发布者 ID（`claude-sonnet-5`）、Bedrock 推理配置文件 ID（`us.anthropic.claude-sonnet-5`）或 Foundry 部署名称。条目可以是纯 ID 字符串或对象。\n\n**网关：** `name` 必须是网关 `/v1/models` 端点返回的精确 ID。如果您在别名（`sonnet`）上设置了 `supports1m`，但服务发现返回的是完整 ID，则该变体不会出现。\n\n**扩展上下文**（`supports1m`）是您针对部署所做的功能断言；仅为您已确认支持 100 万 Token 窗口的模型进行设置：\n```json\n[{\"name\": \"claude-sonnet-5\", \"supports1m\": true}, \"claude-opus-4-8\"]\n```\n`\"claude-sonnet-5[1m]\"` 是相同条目的简写形式。当某个 ID 同时以纯文本和带有 `[1m]` 的形式列出时（如网关所列出的那样），选择器会显示一个带有 1M 变体的模型；请将 `labelOverride` 置于纯 ID 条目上（`[1m]` 拼写上的标签将被忽略）；带有层级标签的条目不会折叠。`prefer1m: true`（若没有 `supports1m` 则无效）在此条目作为默认模型时，使 1M 变体成为选择器的默认选中项；用户仍可手动切换，且显式选择会被保留。在动态发现模式下（未配置显式列表），请改为设置 `modelPrefer1mContext`。\n\n**显示标签**（`labelOverride`）适用于选择器无法推导出友好名称的 ID（Bedrock ARN、网关路由别名）。仅用于显示；应用实际发送的仍是 `name`：\n```json\n[{\"name\": \"arn:aws:bedrock:us-east-1:123:application-inference-profile/abc\", \"labelOverride\": \"Claude Opus (Prod)\"}]\n```\n\n**层级映射**（`anthropicFamilyTier`）告知应用该条目代表哪个 Claude 层级（`haiku`/`sonnet`/`opus`/`fable`/`mythos`），以便纯层级别名（例如在 Code 会话中）解析为您的模型。当多个条目共享一个层级时，`isFamilyDefault: true` 决定首选条目：\n```json\n[{\"name\": \"us.anthropic.claude-opus-4-8\", \"anthropicFamilyTier\": \"opus\"}]\n```",
  "Off unless set: the Usage page shows token counts only": "关闭除非显式设置：用量页面仅显示 Token 计数，因为应用无法知晓您协商的价格。开启后可配置精确的模型计费单价估算。",
  "Mirrors Claude Code's managed `modelPricing.multiplier`": "对应 Claude Code 托管的 `modelPricing.multiplier`：应用于每次计算的模型定价乘数因子（范围为 (0, 1]）。",
  "Each row replaces Anthropic list price for one model": "每一行替换用量页面估算中某个模型的 Anthropic 官方价格（单位：美元 / 百万 Token）。",
  "`available` (the default) lets users install from the Directory's Organization tab": "`available`（默认值）允许用户从目录的“组织”标签页进行安装。`auto_install` 会在启动时自动为所有用户挂载安装。",
  "Prints to stdout one of: a bare token": "向标准输出（stdout）打印以下内容之一：纯文本令牌（作为用户名 `x-access-token` 的 git 密码发送）、用户名和密码，或 HTTP 授权头。",
  "When on (default), users can add plugin marketplaces": "开启时（默认），用户可以从插件浏览器添加插件市场。设置为 `false` 可阻止用户添加未经验证的第三方市场。",
  "When on (default), users can upload plugin files": "开启时（默认），用户可以上传插件文件并与 Claude 一起创建插件。设置为 `false` 可防止本地创建与侧载插件。",
  "If unset, a shared placeholder UUID is used": "如果未设置，将使用共享的占位符 UUID：遥测数据将无法与其他未配置的部署区分开来。",
  "\"Essential\" means the signals Anthropic needs to keep your deployment working": "“基础遥测”是指 Anthropic 保持您的部署正常工作所需的关键信号：**崩溃堆栈**、**核心启动错误**以及会话生命周期诊断。",
  "\"Nonessential\" covers two things: **product-usage analytics**": "“扩展遥测”涵盖两项内容：**产品使用情况分析**（哪些功能被使用、导航路径）以及性能指标。",
  "\"Nonessential services\" covers three outbound fetches": "“非核心网络服务”涵盖应用无需其即可运行的三项出站获取请求：**连接器图标 Favicon**、**插件展示元数据**与公开文档。",
  "For OAuth-authenticated entries, the app builds the redirect URI": "对于采用 OAuth 认证的条目，应用会将重定向 URI 构建为 `http://<callbackHost>:<callbackPort>/callback`。请确保 IdP 允许该重定向端口。",
  "When enabled (the default), approval prompts for tools": "启用时（默认），没有 `toolPolicy` 条目的工具的审批提示会提供一个持久保存的复选框，以允许该工具在当前会话的后续调用中自动放行。",
  "Server-derived: the Claude.ai control plane sets this key": "服务端派生：Claude.ai 控制面板在每次分发配置时设置此键，反映您组织当前的订阅与功能权限层级。",
  "Sets the per-call timeout the agent applies to every MCP tool call": "设置智能体应用于每次 MCP 工具调用的超时时间；运行时间超过该值的调用将以超时错误终止。",
  "Read-only folders can be viewed and searched but not modified": "只读文件夹可以在 Cowork 中查看和搜索，但不能被修改（无论由 Claude 还是通过 UI）。在沙箱中受到严格写保护。",
  "When on (default), users can create new skills": "开启时（默认），用户可以在应用中创建新技能并上传技能文件。设置为 `false` 可在组织内全面禁用自定义技能。",
  "Keys use the same tool names and argument-scoped rule syntax": "键使用与**已禁用的内置工具**相同的工具名称和参数范围规则语法（例如 `Bash(curl *)` 或 `Read(**/.env)`）。",
  "When enabled, users can select **Auto mode**": "启用时，用户可以选择 **自动模式**（Code）/ **自动批准**（Cowork）。Claude 将自主运行而无需反复停下等待人工确认。",
  "Before fetching a page, Claude Code's WebFetch tool asks": "在获取页面之前，Claude Code 的 WebFetch 工具会询问 `api.anthropic.com` 该域名是否在安全白名单内。开启跳过可用于离线或私网环境。",
  "Paths can reference `~` and these environment variables": "路径可以引用 `~` 以及针对每个用户展开的环境变量：`%OneDrive%`、`%USERPROFILE%` 等。可精确约束工作区允许访问的本地磁盘范围。",
  "Sessions started inside a WSL distro run Claude Code": "在 WSL 发行版内部启动的会话会在其原生 Linux 环境中运行 Claude Code，同时继承宿主机的端点与凭据代理配置。",
  "AD Folder Redirection can point %APPDATA% at a network": "AD 文件夹重定向可能将 %APPDATA% 指向网络共享（UNC），导致 Cowork 任务无法启动。此设置可将数据路径重定向到本地高速磁盘。",
  "Applies to **both** Cowork and Code, and only to **tool calls**": "同时适用于 **Cowork** 和 **Code**，且仅适用于**工具调用**。在 Cowork 中它控制沙箱的网络出口；在 Code 中控制 CLI 工具的联网权限。",
  "Pins the app (sign-in, the connection test, model discovery": "将应用（登录、连接测试、模型发现、MCP 服务器、插件）的证书链锁定到指定的 CA 根证书，用于企业自签名代理审计环境。",
  "At launch the app downloads the PAC script": "启动时，应用会下载 PAC 脚本，并像浏览器一样针对每个外发请求向其查询应使用哪个代理服务器。",
  "When on (default), the app resolves the signed-in user's identity": "开启时（默认），应用会从配置的凭据源解析已登录用户的身份，并在侧边栏底部、账户菜单及 Code 会话问候语中予以展示，同时作为 `enduser.id` 属性随遥测事件发出。",
  "Free-text instructions from your organization that Claude Desktop appends": "来自您组织的自由文本指令，Claude 桌面端会在 **Chat**、**Cowork** 和 **Code** 会话中将其附加在自身系统提示词之后（每个对话、任务和会话，包括派生的子智能体）。模型会获知这些指令来自组织管理员，且优先级高于用户的个人偏好。\n\n这是模型遵循的指导原则，而非强制硬性控制：与任何系统提示词文本一样，它引导模型的行为，但不能替代权限限制键（工具策略、网络出口允许名单、文件夹允许名单）。应用的自身系统提示词绝不会被此键替换或缩短；在 Code 会话中，它添加在 Claude Code 自身提示词之后，且任何 `CLAUDE.md` 说明依然生效。\n\n会话启动时从应用加载的配置中读取；更改后的值通常在下次应用启动后对新启动的会话生效。首尾空白将被自动去除；空字符串视为未设置。最多 3,000 个字符；超过限制的值将被拒绝（忽略该键并提示配置错误）而非截断。当值以 JSON、引导响应、`.mobileconfig` 配置文件或 `.reg` 注册表文件分发时，换行符将被完整保留；组策略（ADMX）和 Intune 针对此设置的文本框为单行输入。",
  "Set this to the name users should see for this deployment": "设置用户应在此部署中看到的自定义名称（例如，“企业 AI 专属工作台”）。未设置时，桌面端显示默认的提供商标签。最多 60 个字符。",
  "Use this for compliance notices, an internal-support link": "用于合规性声明通知、内部技术支持链接或标识特定企业部署。横幅会在登录后常驻显示在应用窗口顶部。",
  "Each entry is a Claude Code tool name": "每个条目为一个 Claude Code 工具名称（Bash、Read、Write、Edit、Glob、Grep、NotebookEdit、WebFetch、WebSearch、Task、TodoWrite、TaskCreate、TaskUpdate、TaskGet、TaskList、TaskStop、Skill、REPL、Javascript、AskUserQuestion、ToolSearch、SendUserMessage）或带参数范围的[权限规则](https://code.claude.com/docs/en/permissions#permission-rule-syntax)，例如 `Bash(curl *)` 或 `Edit(**/*.env)`。纯工具名覆盖所有调用；带范围的规则在所有权限模式（包括自动模式与绕过模式）下均覆盖匹配的调用。范围匹配适用于 `Bash(…)`（命令匹配模式），以及写为 `Read(…)`（覆盖读操作）或 `Edit(…)`（覆盖写操作）的文件路径；其他工具采用 `Tool(<field>:<pattern>)` 形式。WebSearch 和 WebFetch 仅限纯名称：按主机的 Web 访问通过 `coworkEgressAllowedHosts` 约束。\n\n带范围的 `Bash(…)` 规则适用于 Code 会话和基于虚拟机沙箱的 Cowork 会话（`requireFullVmSandbox`）；Cowork 自带的沙箱 Shell 仅支持纯名称。将文件模式定位到 `**/`（例如 `Read(**/secrets/**)`），因为在虚拟机沙箱中宿主机绝对路径不匹配。带范围的规则需要跨整套客户端舰队支持的版本（`disableAutoUpdates` 锁定版本）：较旧的内部版本会原样跳过带范围条目。模式包含以空格或逗号分隔的条目仅通过 Claude Code 托管设置渠道强制执行，因此另一个 Claude Code 托管设置源（claude.com）会将其替换，除非该源将 parentSettingsBehavior 设置为 \"merge\"；任何其他条目均会强制执行。\n\n不可用的条目（小写工具名、未闭合的括号、带范围的 WebSearch(_) 或 WebFetch(_)）将被保留，因为拒绝列表完全按原样处理，并引发配置警告。",
  "For `git` sources, the HTTPS remote the app clones": "对于 `git` 来源，为应用克隆的 HTTPS 远程仓库地址。对于 `url` 来源，为 `marketplace.json` 的 HTTPS 地址，其插件为 `archive` 条目（一个 zip `url` 加上其 `sha256`；`ref` 和 `path` 不适用）。归档 URL 必须与清单 URL 同源：该源站是防火墙上唯一需要放行的主机，也是唯一会发送凭据的主机。如果任何归档包下载或校验失败，该次获取的所有内容均不会安装，并将在下次同步时重试。\n\n当“安装策略”为 `auto_install` 或 `required` 时，请将 `manifestSha256` 设置为该文件的精确 SHA-256 哈希，并为每个归档包提供 `sha256`：具有不同摘要的服务端清单会被拒绝，未固定哈希的归档包会被跳过，因此发布新清单意味着必须更新 `manifestSha256`；若完全未配置 `manifestSha256`，该条目将被视为 `available`（并在配置中给出警告提示）。从您的推理网关或引导服务器自有源站提供的市场，可以将“安装策略”保留为 `available`，并在清单中将各个插件标记为 `\"installationPreference\": \"auto_install\"`（每个插件附带一个 `sha256`）；这些插件将在无需哈希固定的情况下安装，并在定期重新获取时拾取新清单。在任何其他源站上，针对单个插件的此类标记都将被忽略。\n\n`credentialKind` 用于选择发送的凭据类型：`anonymous`（默认）不发送任何凭据，`userGit` 将用户在该主机上存储的 git 凭据作为 HTTP Basic 发送，`credentialHelper` 将辅助程序的输出作为 `Authorization` 发送，而 `inferenceCredential`（仅限 `url` 来源）使用应用已向您的推理网关或已启用的引导服务器发送的凭据，因此清单必须位于这两个源站之一；当尚无凭据可发送时，不会发起请求，且该条目会报告原因。",
  "Leave off if Scope already includes `offline_access`": "如果作用域（Scope）中已包含 `offline_access` 或服务器拒绝该参数，请保持关闭。\n\n使用显式端点（授权 URL / 令牌 URL，无服务发现）时，此开关无法自行生效——应用无法确认授权服务器（AS）是否宣告支持 `offline_access`，因此请直接在**作用域**中包含 `offline_access` 以实现静默刷新。",
  "How long the app may reuse the helper's output before running it again": "应用在重新运行辅助程序之前可以复用其输出的时长。该检查仅在服务器进程（重新）启动时执行。环境变量在进程创建时即已固定，运行中的服务器在下次重启之前绝不会看到刷新后的值。请将此值设置为小于或等于您的令牌有效期：因令牌过期而陷入崩溃循环的服务器，在 TTL 到期前可能会使用相同的缓存环境重新生成。默认值：300 秒。",
  "With Continuous Access Evaluation the server declares the `cp1`": "启用持续访问评估（Continuous Access Evaluation, CAE）后，服务器会声明 `cp1` 客户端功能，因此 Microsoft Entra 会颁发生命周期长达约 28 小时的访问令牌；当管理员撤销会话、停用账户或用户风险等级发生变化时，系统会通过声明质询（Claims Challenge）在数分钟内将其阻断——如果租户强制执行了指定 IP 位置或 Global Secure Access 合规网络条件访问策略，当令牌从该网络外部提交时也会被阻断。未配置此类策略的租户可获得更长的令牌生命周期和快速撤销能力，但无法获得基于网络的安全重放防护；设置为 `disabled` 可在所有登录流程中保持标准的一小时非 CAE 令牌。变更仅适用于连接器带着新配置重新生成后颁发的令牌；已缓存的长效令牌将继续使用直至过期（断开连接 → 重新连接可执行全新登录）。",
  "For inference providers without native web search": "适用于不具备原生网络搜索能力的推理提供商（如 Bedrock、自定义网关）。在 Anthropic API 和 Vertex 上，模型的内置网络搜索开箱即用，无需配置此项。\n\n提供商的 API 密钥属于敏感凭据——建议使用**请求头辅助脚本**打印其认证标头，而不是直接存储在 `headers` 中：\n- **Brave** — `X-Subscription-Token`\n- **Exa** — `x-api-key`\n- **Tavily** — `Authorization` (Bearer …)\n\n如需彻底关闭网络搜索，或要求对每次搜索进行审批，请在 `disabledBuiltinTools` 或 `builtinToolPolicy` 中列出纯工具名称 `WebSearch`；它不需要配置主机作用域。",
  "Absolute path to an executable that prints a single JSON object of request headers": "可执行文件的绝对路径，该程序须向标准输出（stdout）打印包含请求标头的单个 JSON 对象，例如 `{\"X-Subscription-Token\": \"…\"}`。应用会在每次请求前运行它，并按下方设置的 TTL 缓存结果。使用此方式可从机密管理器（Secrets Manager）中动态拉取密钥，避免在 `headers` 中内联明文存储。",
  "In **access-token** mode the token's audience must be your gateway": "在 **access-token** 模式下，令牌的受众（Audience）必须是您的网关。在通过请求的作用域（Scope）来决定受众的身份提供商（**Entra ID**）中，请在 `scopes` 中包含网关的 API 资源作用域——例如 `api://<gateway-app-id>/.default`，或通过*公开 API* 暴露的指定委托作用域。如果仅包含标准 OIDC 作用域（`openid profile email offline_access`），Entra 将返回一个 v1.0 Microsoft Graph 令牌，其签发者（`https://sts.windows.net/<tenant>/`）与此处配置的 v2.0 签发者不匹配，因此校验 `iss` 的网关会拒绝该令牌。而在带外设置访问令牌受众的身份提供商（如 Keycloak 受众映射器、Auth0 默认受众、Okta 自定义授权服务器）中，则不需要在 `scopes` 中配置资源作用域——受众由身份提供商端的配置直接决定。",
  "Turn off only if your authorization server rejects `offline_access`": "仅在您的授权服务器将 `offline_access` 视为未知作用域并予以拒绝时才关闭；此时请在**作用域**中改用服务器自有的刷新令牌作用域。",
  "Set this only for an IdP that implements RFC 8707 resource indicators": "仅适用于实现了 RFC 8707 资源指示器（Resource Indicators）的身份提供商。配置后，应用会在授权、令牌和刷新请求中发送 `resource=<value>`，从而使 IdP 颁发的访问令牌将受众严格限制在网关本身，而非 IdP 自有的 API。请配合 `bearerTokenType: access_token` 使用；在 `id_token` 模式下，ID 令牌的受众始终为 `clientId`。使用系统代理登录流程（`inferenceGatewayOidcAuthFlow: broker`，仅限 Entra）时此项将被忽略。\n\n**Microsoft Entra ID：** 保持未设置——v2.0 端点会拒绝 `resource` 参数；请改为在 `scopes` 中请求网关的 API 作用域（`api://<gateway-app-id>/.default`）。**AD FS：** 该值会与信赖方信任（Relying-party Trust）的标识符进行匹配；请在信任中添加一个 `https://` 标识符（例如网关 URL）并在此处使用。必须为绝对 `https://` URL；且不得包含 URI 片段（根据 RFC 8707，IdP 会将其作为 `invalid_target` 拒绝）。更改此设置将要求用户重新登录。",
  "Each entry is `{ name, description?, url, sha256? }`": "每个条目为 `{ name, description?, url, sha256? }`：包含技能 ZIP 包或 `SKILL.md` 的 HTTPS（非回环）URL（配置服务器可以分发预签名的存储 URL；托管控制台会拒绝带有凭据的 URL），以及该文件的 SHA-256 哈希值，任何内容不符的下载都会被拒绝。客户端无法读取的条目或与前面名称重复的条目会被单独丢弃并记录报告，其余条目正常安装。目前由 Microsoft 365 的 Claude 加载项读取（配置服务器通过这种方式向加载项分发技能）。Claude 桌面端目前通过插件市场（`plugins.*`）分发技能，未来可能会采用此列表。",
  "Code sessions export over the protocol set here": "Code 会话将通过此处设置的协议进行导出。在 Windows 上，以及在其他平台上只要 Claude Code 引擎被配置了 HTTP 代理（操作系统代理、`egressProxyUrl`、`egressProxyPacUrl`，或 Claude Code 配置文件中的 `HTTPS_PROXY` / `HTTP_PROXY`），Chat 和 Cowork 任务都会改用 `http/protobuf` 而非 `grpc` 进行导出；应用日志会记录该替换。桌面应用自身的事件始终通过 `http/json` 发送至 `<endpoint>/v1/logs`。这些都不会改变端点地址，因此仅当收集器在同一地址同时提供 OTLP/HTTP 服务时才选择 `grpc`；否则请保持 `http/protobuf` 并将 `otlpEndpoint` 指向收集器的 OTLP/HTTP 接收端（通常为端口 4318）。",
  "Pins which OpenSSH client the app runs wherever it starts `ssh`": "固定应用在任何启动 `ssh` 的地方所运行的 OpenSSH 客户端：包括评估用户的 SSH 配置（`ssh -G`）、建立 SSH 连接及其通道，以及 Code 标签页的终端。存在时，`ssh-keygen` 和 `ssh-add` 会从同一目录中获取，否则从 PATH 环境变量中获取。该程序必须是 OpenSSH 7.6 或更高版本（在 Windows 上，承载连接须为 Win32-OpenSSH 9.4 或更高版本）；在 macOS 和 Linux 上接受以此类程序结尾的包装脚本，而在 Windows 上必须是原生 `.exe`（不得为 .cmd、.bat 或 .ps1 脚本）。设置此键后，如果该程序缺失、无法运行或版本过旧，SSH 会话将失败并提示用户联系其 IT 管理员（配置的路径包含在详细信息中）——应用绝不会回退到其他 ssh。连接本身通过系统 OpenSSH 引擎上的此程序运行，SSH 连接引擎设置的 `system-openssh` 选项在所有平台（包括 Windows）上均会选中该引擎；当应用改为使用内置 SSH 库建立连接时，此键仍控制配置评估（`ssh -G`）、主机密钥查找（`ssh-keygen`）和终端。",
  "`system-openssh`: the app makes every SSH connection by running an OpenSSH `ssh` program": "`system-openssh`：应用通过运行 OpenSSH `ssh` 程序来建立每个 SSH 连接——使用 `sshClientPath` 指定的程序，否则使用用户 PATH 上的第一个 `ssh`（在 Windows 上为 Win32-OpenSSH `ssh.exe`：PATH 上的优先，否则使用系统自带或微软安装的客户端）——因此认证使用的是组织自有的 OpenSSH 构建版本及其 Kerberos/GSSAPI、证书和 `ssh_config` 支持。该程序必须是 OpenSSH 7.6 或更高版本（Windows：Win32-OpenSSH 9.4 或更高版本）。当设置了 `sshClientPath` 且该程序不可用时，会话将失败并报错提示用户联系其 IT 管理员，而不会静默回退；未设置且 Windows 没有可用客户端时，将使用内置库。\n\n`builtin`：无论内部版本默认值为何，均由应用的内置 SSH 库建立连接。\n\n`auto` 或未设置：使用内部版本的默认引擎。\n\n显式指定的值适用于新连接（已连接的会话保留其当前引擎），并在两个方向上覆盖内部版本默认值，包括 Anthropic 为 OpenSSH 引擎下发的任何远程关闭开关——因此在设置了 `system-openssh` 的情况下，可通过在此处设置为 `builtin` 来切回内置引擎。",
  "A value that the app does not recognize turns Chat, file analysis, and Cowork off": "应用无法识别的值将关闭 Chat、文件分析和 Cowork，效果与 `off` 相同。在部署新值之前，请先升级设备上的客户端版本。",
  "Set it via MDM (plist, registry, or file) or serve it from your configuration endpoint": "可通过 MDM（plist、注册表或文件）设置，或从配置端点提供（远程引导配置）。当运行中的应用检测到必须重启才能生效的托管配置变更时，会显示侧边栏重新启动卡片并启动此窗口。该窗口从运行中应用的下一次配置重新检查（即相邻的重新检查间隔）开始计时，因此在保存更改与弹出重启对话框之间，请在此值的基础上预留最多一个重新检查间隔的时间。窗口结束时，应用将弹出阻断式的重启对话框，并在无活动（没有正在运行的 Claude 任务且无键盘或指针输入）2 分钟后自动重启；用户也可以立即重启。默认值为 24 小时。可设置更大的值（最高 336 = 14 天）以给用户留出更充裕的时间；设置为 `0` 则在首次检测到变更时立即显示对话框。\n\n服务端下发的值从最新的服务端配置中读取，因此缩短或放宽宽限窗口会在下一次轮询时生效而无需重启，且仅更改此键绝不会要求重启。与相邻的更新键类似，仅设置应用行为键的设备管理描述文件中的值可在不使其余配置变为设备托管的情况下生效，且优先级高于服务端提供的值。由于该键与其他应用行为键归为一组，设置其中任意键的描述文件将接管整个分组：请将此键与您部署的更新键配置在同一个描述文件中，否则在这些设备上服务端的值将被忽略，并套用 24 小时默认值。",
  "How often the running app re-checks its managed configuration for changes": "运行中的应用重新检查其托管配置变更的频率：它会通过条件请求重新轮询您的配置端点，因此未发生变更的配置仅消耗一次 `304` 往返。检测到变更时会显示侧边栏重新启动卡片并启动 `relaunchEnforcementHours` 窗口，因此保存的变更大约在一个时间间隔内传达至运行中的应用。默认值为 10 分钟；每次等待均有 ±10% 的抖动，以避免集群同步轮询。超出 2–30 的值将被视为解析错误而拒绝，并套用默认值。\n\n无需重启即可应用：新的服务端下发值会在收到该值的检查时重置计时器，仅更改此键绝不会要求重新启动。可通过 MDM 设置或从配置端点提供；与相邻的更新键类似，仅设置应用行为键的设备管理描述文件中的值可在不使其余配置变为设备托管的情况下生效，且优先级高于服务端提供的值。由于该键与其他应用行为键归为一组，设置其中任意键的描述文件将接管整个分组，随后该组中的每个键都仅从该描述文件中读取：请将此键与更新键（`disableAutoUpdates`、`autoUpdaterEnforcementHours` 等）部署在同一个描述文件中。设置了更新键但未设置此键的描述文件会忽略服务端间隔并应用默认值；仅设置此键的描述文件会忽略服务端更新设置，导致服务端提供的 `disableAutoUpdates` 在这些设备上失效。同时管理连接本身（设置 `bootstrapUrl` 或提供商键）的描述文件遵循常规层级顺序：一旦获取到服务端配置，将由此配置（而非描述文件）提供此键。",
  "When set to `true`, sessions cannot run in bypass permissions mode": "设置为 `true` 时，会话无法在绕过权限模式下运行：应用将不再在 Code 和 Cowork 中提供该模式，强行请求该模式的会话将被降级。这是 Claude Code 托管设置的 `permissions` 节中的 `disableBypassPermissionsMode` 设置；由其他来源单独部署的 Claude Code 托管设置文件若将其设为 `\"disable\"` 也会移除该模式，无论哪个来源生效。\n\n未设置（默认）：部署策略允许的用户可以选择绕过权限模式。",
  "When set to `true`, Code sessions refuse reads outside their working directories": "设置为 `true` 时，Code 会话将拒绝读取其工作目录（会话文件夹加上任何 `allowedWorkspaceFolders`）以外的内容。文件工具（Read、Grep、Glob）在所有权限模式下均拒绝这些读取；在运行 Claude Code 沙箱的环境中（macOS，或配置了 `allowedWorkspaceFolders` 或出站白名单且带 bubblewrap 的 Linux 与 SSH 主机），Shell 命令无法看到主目录和其他用户文件夹（`/Users`、`/home`、挂载卷），且对那里的读取会被直接拒绝而不会弹出提示；在其他环境（Windows、不带 bubblewrap 的 Linux，或既未配置文件夹也未配置出站白名单），此类 Shell 读取会弹出审批提示。应用仍保持用户的 git 配置文件（其自身可能嵌入凭据如 URL 令牌；符号链接的文件保持隐藏）、应用自身的 Claude Code 安装目录，以及会话的插件和附件文件夹可读（Windows SSH 主机除外，在此类主机上受拦截的插件文件和附件处于文件工具无法访问的状态）。如果允许的文件夹本身是或包含主目录，则主目录保持可读。\n\n用户可以在其自有的 Claude Code 设置中使用 `sandbox.filesystem.allowRead` 或 `permissions.additionalDirectories` 重新开放文件夹（甚至是整个主目录）；由 git 仓库跟踪的设置文件则无此权限。该键通过 Claude Code 托管设置通道传输：除非其他 Claude Code 托管设置来源将 `parentSettingsBehavior` 设置为 `\"merge\"`，否则该来源会替换此键；而将其设置为 `sandbox.filesystem.allowManagedReadPathsOnly` 的来源会将其降级为审批提示。这对应 Claude Code 托管设置中的 `permissions.blockReadsOutsideWorkingDirectories`。\n\n未设置（默认）：不作任何更改。",
  "When set to `true`, the Cowork workspace VM on macOS and Windows gets a static IPv6 address": "设置为 `true` 时，macOS 和 Windows 上的 Cowork 工作区虚拟机将在其虚拟网络上获得一个静态 IPv6 地址（唯一的本地 `fd…` 地址）和与其 IPv4 地址并存的 IPv6 默认路由，虚拟机的网关会像处理 IPv4 一样通过设备自有的 IPv6 连接转发该流量。当智能体在虚拟机中运行的工具（Shell 命令、软件包安装、`git`、插件命令，以及在 `requireCoworkFullVmSandbox` 下的完整引擎）必须访问纯 IPv6 目标时，请启用此项。随后虚拟机的解析器也会返回 IPv6（AAAA）解析记录。虚拟机建立的 IPv6 连接仅在设备自身的 IPv6 正常工作时才会成功；在没有可用 IPv6 的设备上，同时支持双栈的目标将继续通过 IPv4 正常工作，而纯 IPv6 目标仍不可达。由于虚拟机的地址是唯一本地地址，其中的大多数工具在访问双栈目标时仍会优先选择 IPv4，因此 IPv6 主要用于承载发往纯 IPv6 目标的流量。\n\n这是连通性设置，而非出站访问控制：`coworkEgressAllowedHosts` 依然通过主机名决定智能体工具可以访问哪些主机（无论使用哪种协议），且此处仍不接受 IPv6 字面量地址。您的策略所允许的主机也必须可访问，并按照您在网络上期望的方式通过 IPv6 进行过滤。\n\n未设置（默认）：虚拟机为纯 IPv4 环境，且其解析器不返回任何 IPv6 应答。更改将在下次工作区虚拟机启动时生效，通常在下一次应用启动时。不适用于 Linux 上的 Cowork 工作区虚拟机或 Code 会话，后者使用设备自身的网络栈。",
  "`auto` (default): use the OS sign-in broker where available": "`auto`（默认）：在可用时使用操作系统登录代理（Windows 上的 WAM、macOS 上的“公司门户”SSO 扩展），否则回退到浏览器登录。`disabled`：始终使用浏览器登录。`required`：当系统代理不可用时使登录失败，而不是回退到浏览器，以确保刷新令牌始终由系统代理保管。Linux 没有系统代理，因此不支持 `required`。早于引入 `required` 的旧版桌面客户端会将其视为 `disabled`（仅浏览器登录）——这恰恰是相反的行为——因此请根据客户端版本分批推进部署。",
  "When enabled, Cowork and Code sessions get a built-in browser pane": "启用后，Cowork 和 Code 会话将获得一个内置浏览器面板。用户可在其中打开任何网站，而在用户审批通过每个站点后，Claude 可以打开网页、阅读网页并使用其浏览器工具执行操作（专用网络地址会对每次操作重复询问，除非用户选择始终允许该主机）。页面直接从用户的计算机加载，因此您的网络控制策略依然适用；面板中的登录凭据和 Cookie 会保存在设备上的应用浏览器配置文件中，直至用户清除它们。\n\n关闭（默认）：不提供浏览器面板，且 Code 会话为开发服务器保留仅限本地主机（localhost）的预览。在 Anthropic 控制平面（混合）下，像配置其他键一样在桌面部署表单中设置此键。在 Claude 处理公共网站之前，应用会在不登录的情况下向 Anthropic 位于安全列表端点的主机核对该站点：请求携带去除查询参数的页面地址，不包含任何账户、组织或设备标识符；列表中的站点对 Claude 始终保持拦截，无论下方配置了哪些键。专用地址、内部后缀或单标签名称以及解析为专用地址的名称绝不会被发送（即使仅有您的 VPN 能解析公共域名下的名称，也会对其进行核对）。如果该安全核对主机不可达，用户仍可正常浏览，但 Claude 的页面工具在该站点将被停用。您可以通过站点策略键（`builtinBrowserDefaultDomainPolicy`、`builtinBrowserAllowedDomains`、`builtinBrowserBlockedDomains`）限制 Claude 可以浏览的范围，这些策略与管理控制台中的 Claude in Chrome 站点策略保持一致。单独部署的 Claude Code 托管设置文件在此键之上仍然生效：`disableBrowserExternalNavigation: true` 即使在此键开启时也会保持浏览器处于关闭状态，而 `browserExternalPageTools: \"disabled\"` 会保留可用面板但关闭 Claude 对外部站点的页面工具。在应用重启后生效。",
  "Applies when `builtinBrowserEnabled` is on, in both Cowork and Code": "在 Cowork 和 Code 会话中当 `builtinBrowserEnabled` 开启时适用，用于决定 Claude 可以使用其浏览器工具打开、读取或操作哪些网站；用户本人仍可自行查看任何网站。`allow`（默认）：允许除 `builtinBrowserBlockedDomains` 中的条目外的所有网站。`block`：禁止除 `builtinBrowserAllowedDomains` 中的条目外的所有网站。在任一策略下，当 `coworkEgressAllowedHosts` 设置为除 `*` 以外的任何值时均构成外层边界：该边界之外的网站在浏览器中也对 Claude 禁用。无法读取的值在修复前将被视为 `block` 处理。\n\n此键和两个站点列表与管理控制台中的 Claude in Chrome 站点策略保持一致。在 Anthropic 控制平面（混合）下，它们遵循组织的 Claude in Chrome 设置，无法单独设置。",
  "Used when `builtinBrowserDefaultDomainPolicy` is `block`": "在 `builtinBrowserDefaultDomainPolicy` 为 `block` 时使用；在 `allow` 下被忽略。匹配条目的网站是 Claude 可以使用其浏览器工具打开并进行操作的站点；所有其他外部网站均被视为已被您的组织阻止——Claude 无法打开它，无法对其进行读取或操作，且从 Claude 正在处理的页面向其嵌入的框架（iframe）、弹出窗口和重定向均会被拒绝。用户本人仍可手动输入地址并查看它。\n\n条目使用与 `coworkEgressAllowedHosts` 相同的语法，但省略了单纯的 `*`：正如在 Claude in Chrome 中一样，允许站点条目中不允许直接开放所有网站——如需开放请将策略设置为 `allow`。以公共后缀为基础的通配符（如 `*.co.uk`、`*.github.io`）同样会被忽略，以确保条目永远无法开放整个共享注册表域。超出该语法的任何其他条目将被保留但不匹配任何内容（应用日志和编辑器中会标明）；完全无法读取的值在修复前将不开放任何内容。`localhost` 开发服务器始终可访问，因此将其列入不会产生任何变化。\n\n留空或未设置（默认）：在 `block` 策略下，Claude 不得打开任何外部网站。在 Anthropic 控制平面（混合）下，此项遵循组织的 Claude in Chrome **允许的站点**列表，无法单独设置。",
  "Used when `builtinBrowserDefaultDomainPolicy` is `allow` (the default)": "在 `builtinBrowserDefaultDomainPolicy` 为 `allow`（默认）时使用；在 `block` 下被忽略。匹配条目的网站将被视为已被您的组织阻止：Claude 无法打开它，无法使用其浏览器工具对其进行读取或操作，且从 Claude 正在处理的页面向其嵌入的框架（iframe）、弹出窗口和重定向均会被拒绝。用户本人仍可手动输入地址并查看它——面板会显示“已被您的组织策略阻止”横幅，且 Claude 的工具在该站点保持停用；若要同时对用户收回外部浏览器访问权限，请改为部署 Claude Code 的 `disableBrowserExternalNavigation` 托管设置。\n\n条目使用与 `coworkEgressAllowedHosts` 相同的语法；`*` 将阻止所有外部网站。超出该语法的条目将被保留但不匹配任何内容（应用日志和编辑器中会标明）；完全无法读取的值在修复前将阻止所有外部网站。`localhost` 开发服务器绝不会受到影响。\n\n在允许的出站通配符内配置例外情况，或在出站完全开放时使用此键。留空或未设置（默认）：除出站列表外不阻止任何网站。在 Anthropic 控制平面（混合）下，此项遵循组织的 Claude in Chrome **阻止的站点**列表，无法单独设置。",
  "Meant to be set per user or group, through the served configuration's group overrides": "旨在通过下发的配置组替代（Group Overrides）按用户或用户组设置，或在承载其余配置的同一设备管理描述文件中设置（如同任何策略键一样，仅携带此键的描述文件即可使设备变为由描述文件托管）；当服务端配置未重申该保留策略时，设备描述文件中的保留设置同样有效。删除操作在首次获取到携带此值的配置时即告停止，早于下次重启时作为生效配置起效；无法连通配置服务器的设备不会删除任何内容。"
}
