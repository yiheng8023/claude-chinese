# Claude Chinese Toolkit v1.2.66 发布说明 (Release Notes)

🎉 本次版本全面适配官方大版本跃升的 **Claude Desktop v2.7032.0.0**，以手术刀级别彻底根除了导致特定会话报错崩溃的致命 Bug，并对新版本新增功能、长文档与核心设置面板进行了出版级地毯式汉化补齐，全库有效校验词条跃升至 **21,221 条**！

---

## 🚀 核心修复与更新亮点 (Key Highlights & Fixes)

### 1. 根治会话加载崩溃致命缺陷 (Eliminate Session Loading Crash)
- **精准根除**：排查并彻底拔除了在特定会话中引发 `Something went wrong loading this session.` 红色报错的严重冲突；
- **AST 变量拓扑修复**：针对上游版本代码重排，重构了会话菜单子项的正则匹配引擎，淘汰了对 React 组件结构破坏性的固定变量引用，采用全动态自闭环 AST 变量捕获，杜绝了数组误传入 JSX 导致的 React 渲染引擎熔断。

### 2. 补齐 v2.7032.0.0 核心新增功能与设置面板 (Capabilities & Settings)
- **功能中心**：精准映射官方新提取的标准化字段 `Capabilities` ➔ **`功能`**，统一各级菜单与重定向跳转指引；
- **定时审批策略**：深度汉化 `Scheduled-task tool approval lifetime` ➔ **`定时任务工具审批有效期`**，补齐所有折叠提示与时间周期控制项；
- **交互演进适配**：完美适配新版会话生命周期与设置项从导航栏向标题栏下拉箭头 `v` 的结构迁移。

### 3. 企业级长文档库扩充 (12 篇新增官方长文档出版级汉化)
- 地毯式扫描并收录 v2.7032.0.0 新增的整整 12 篇官方深度长文档：
  - MCP 工具持久性审批安全指南 (Persistent Approval Guidelines)；
  - 任务执行防睡眠机制与电源策略 (Keep Awake Policy)；
  - 企业启动器配置与工作空间隔离 (Enterprise Launcher)；
  - OIDC 联合身份认证与连接器状态管理等；
- 全库收录并汉化的官方折叠长文档扩充至 **125 篇**！

---

## 🛡️ 工业级质量门禁 (Quality Assurance)
- **有效词条总规模**：**21,221 条**（Shell 字典 705 + Ion 核心词库 20,468 + Dynamic 动态词库 48）；
- **长文档拦截器**：125 篇长文档 100% 具备多签名自适应拓扑抗混淆特性；
- **事后与事前兼容矩阵**：真实上游混淆变异测试与全平台生命周期自动化回归测试 **100% 绿灯**！

---

## 📦 安装与使用 (Installation)

### Windows 用户 (极速一键安装)
1. 下载最新发行包或拉取仓库最新代码；
2. 双击或右键运行 **`install.bat`** ➔ 选择 **“以管理员身份运行”**；
3. 提示安装成功后启动 Claude 桌面客户端，即刻体验丝滑出版级全中文界面！

> **💡 温馨提示**：在 Windows 控制台（cmd）运行期间，若鼠标不小心点击了控制台窗口导致出现 `选择 管理员:...` 文本选中冻结，只需按下 **空格键 (Space)** 或 **回车键 (Enter)** 即可立即解冻继续极速运行！
